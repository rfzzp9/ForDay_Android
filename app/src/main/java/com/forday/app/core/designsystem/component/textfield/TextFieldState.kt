package com.forday.app.core.designsystem.component.textfield

/**
 * 입력 필드 상태
 */
enum class TextFieldState {
    INACTIVE,    // 비활성화 (입력 전)
    ACTIVE,      // 활성화 (입력 중)
    DISABLED     // 비활성화 (입력 불가)
}